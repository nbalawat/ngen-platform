"""Agent Runtime — standalone agent execution environment.

The agent runtime is provided by the workflow engine's agent manager.
Agents are created, invoked, and managed via the workflow engine's
/agents REST API. This package re-exports the agent manager components
for convenience.
"""
